#ifndef DISPPROTO_T_H
#define DISPPROTO_T_H

#include "config.h"

// TBD

/*
 * DISPLAY DATA EXCHANGE PROTOCOL:
 * in the first element of the array we
 * have a number indicating whether the GUI has
 * to draw the '-' in the display;
 *
 * disp_t[0] = 0  --> don't draw '-'
 * disp_t[0] != 0 --> draw '-'
 *
 * in the other 10 elements of the array
 * we have a number indicating which segment
 * the GUI has to draw
 *
 * disp_t[1] =
 */

#endif
